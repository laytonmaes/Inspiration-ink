import React from "react";
import "./RandomChoice.css"

const RandomChoice = ({userChoice}) => {
    return (
        <section>
            <p>this is choice {userChoice}</p>
        </section>
    )
}

export default RandomChoice